# TODO

- Replace per-message task markdown files with a database-backed task table.
  Keep a lightweight “latest task” snapshot for debugging only.
- Add Supabase CLI integration for complex backends/auth (shared org, per-tenant projects).
